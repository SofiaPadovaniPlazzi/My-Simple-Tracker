# macOS App

This folder contains a lightweight macOS app wrapper for My Simple Tracker.

Open `My Simple Tracker.app` to start the local tracker server and launch the app in your browser.

The app uses the `my_simple_tracker.py` copy bundled inside `Contents/Resources`.
